const myType = { min: 1, max: 200 };
//You can check typeof with
const check = (val) => {
    if (typeof val === "object") {
        console.log("Yes");
    }
    console.log("No");
};
function save(source) { }
